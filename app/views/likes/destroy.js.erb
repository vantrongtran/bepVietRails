<% flash.each do |message_type, messages| %>
  <% if messages.class == String %>
    showNotification("<%= message_type %>", "<%= messages %>");
  <% else %>
    <% messages.each do |message| %>
      showNotification("<%= message_type %>", "<%= message %>");
    <% end %>
  <% end %>
<% end %>
<% if @result %>
  $('#<%= "like-#{@like.target.class.name.downcase.downcase.tr(":", "")}-#{@like.target.id}" %>').html(
    '<%= j render "likes/like", object: @like.target %>'
    );
  readyButtonLikeAction();
<% else %>
  unlikeFaid('#<%= "like-#{@like.target.class.name.downcase.downcase.tr(":", "")}-#{@like.target.id}" %>');
<% end %>
