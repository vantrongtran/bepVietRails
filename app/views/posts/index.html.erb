<% provide :main_class, "index-page" %>
<%= render "layouts/header" %>
<div class="main main-raised">
  <div class="section section-basic fixed_body container">
    <div class="row">
      <div class="headline">
        <h3>Tips</h3>
      </div>
      <div class="row">
        <%= render @posts %>
        <div class="paginate">
          <%= paginate @posts %>
        </div>
      </div>
    </div>
  </div>
</div>
